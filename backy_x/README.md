# Backy X

Backy X is a multi-platform backup software (macOS, Windows, Linux).

## Build Instructions

CMake is required to build Backy X.

Configure the project:
```bash
cmake -S . -B build
```

Build the project:
```bash
cmake --build build
```

## How to Run

### CLI
(Placeholder) Example:
```bash
./build/backy_x_cli --src /path/to/source --dst /path/to/destination
```
